# WashOnline — COS Junior

## 📎 Prérequis
- Node.js et npm  
- Java 17+ et Maven  
- Docker (optionnel)

## 🖥️ Frontend
cd washonline-front
npm install
npm start

## ⚙️ Backend
cd washonline-back
./mvnw spring-boot:run

## 🐳 Docker (optionnel)
docker-compose up --build
